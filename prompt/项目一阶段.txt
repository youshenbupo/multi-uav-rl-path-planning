请执行阶段 1：审计 MATLAB 版 CA-HGALO 项目。

MATLAB 项目目录：

D:\yolo\multiuav\legacy_hgalo

Python 新项目目录：

D:\yolo\multiuav\reinforcement_learning

本阶段目标是理解 MATLAB 代码结构，建立迁移清单，不实现强化学习。

请完成以下任务：

一、扫描 MATLAB 项目

1. 递归列出 legacy_hgalo 中所有文件。
2. 找出主要入口脚本。
3. 找出 S1、S2、S3、S4 场景定义。
4. 找出以下模块对应的 MATLAB 文件和函数：
   - 地形生成和地形高度插值；
   - 起点与终点定义；
   - 圆柱威胁区域；
   - 球坐标路径编码和解码；
   - 路径重采样；
   - 路径长度；
   - 地形净空；
   - 转角约束；
   - 威胁距离；
   - 空间分离；
   - 同步时间冲突；
   - 到达时间一致性；
   - 几何可行性修复；
   - 冲突图构建；
   - 起飞时间调度；
   - 局部横向和高度修复；
   - PSO；
   - GWO；
   - HGALO；
   - CA-HGALO；
   - 成功判定；
   - 实验统计和绘图。
5. 分析主入口与各函数之间的调用关系。
6. 标记是否存在重复版本、废弃文件或名称相近但实现不同的函数。
7. 找出随机数初始化方式和随机种子设置。
8. 找出所有关键单位：
   - 距离；
   - 时间；
   - 速度；
   - 角度；
   - 高度；
   - 代价。
9. 找出 MATLAB 中所有关键数组维度和数据排列方式。
10. 特别检查 MATLAB 的 1-based 索引、角度制和弧度制、行向量和列向量约定。

二、输出文档

在新项目中创建：

docs/legacy_inventory.md
docs/matlab_python_mapping.md

legacy_inventory.md 至少包括：

- 完整目录概览；
- 主入口；
- 核心函数；
- 调用关系；
- 场景文件；
- 输出文件；
- 可能的废弃或重复实现；
- 当前无法确认的内容。

matlab_python_mapping.md 使用表格列出：

MATLAB 文件/函数
Python 计划模块
输入
输出
单位
数组维度
迁移优先级
是否需要 MATLAB 回归数据
风险点

三、限制

1. 不修改 legacy_hgalo。
2. 不创建 MAPPO、GNN 或 CBF 代码。
3. 不猜测未找到的算法内容。
4. 不仅根据文件名判断，需要阅读核心 MATLAB 文件。
5. 完成审计后停止。

最后报告：
- 识别到的主入口；
- 最关键的 10 个 MATLAB 文件；
- 建议的迁移顺序；
- 当前需要我确认的问题。